# 🐺 狼纪录片学习站 - 部署指南

## 快速部署（3步完成）

### 第1步：创建 GitHub 仓库

1. 访问：https://github.com/new
2. 填写以下信息：
   - Repository name: `wolf-docs-hub`
   - Description: `狼纪录片学习站 - Wolf Documentary Hub`
   - Public: ✅ (选择公开)
   - 勾选 "Add a README file"
3. 点击 **Create repository**

### 第2步：上传代码到 GitHub

打开终端，运行以下命令：

```bash
cd /Users/binxie/clawd/wolf-docs
git branch -M main
git remote add origin https://github.com/[你的GitHub用户名]/wolf-docs-hub.git
git push -u origin main
```

**重要**：把 `[你的GitHub用户名]` 替换成你的实际用户名

如果提示需要登录，运行：
```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

### 第3步：启用 GitHub Pages

1. 访问你刚创建的仓库页面
2. 点击 **Settings** (右上角齿轮图标)
3. 左侧菜单找到 **Pages**
4. 在 **Source** 部分：
   - Build and deployment 选择 `Deploy from a branch`
   - Branch 选择 `main`
   - Folder 选择 `/ (root)`
5. 点击 **Save**

### 第4步：等待部署完成

等待 1-3 分钟，刷新 Pages 页面，你会看到类似这样的网址：

```
https://[你的用户名].github.io/wolf-docs-hub/
```

🎉 **部署完成！** 现在任何人都可以访问这个网址了。

---

## 更新网站内容

要添加更多狼纪录片，只需编辑 `index.html` 文件，然后运行：

```bash
git add .
git commit -m "添加新纪录片"
git push
```

GitHub 会自动更新网站（约 1-2 分钟）

---

## 自定义域名（可选）

如果你有自己的域名，可以在 GitHub Pages 设置中添加自定义域名。

---

## 问题排查

### Q: 推送时提示 authentication failed
A: 需要配置 GitHub 凭证。运行：
```bash
git config --global credential.helper store
```
然后再次 push 时输入 GitHub 用户名和 token

### Q: GitHub Pages 显示 404
A: 等待 2-3 分钟，如果还是 404，检查：
- 仓库是否为 Public
- Pages 设置是否正确（main 分支，root 文件夹）

### Q: 想要修改网站样式
A: 直接编辑 `index.html` 文件，然后 git push 即可

---

## 免费特性

✅ 完全免费
✅ 自动部署
✅ 支持自定义域名
✅ HTTPS 自动启用
✅ 无流量限制
✅ 全球 CDN 加速

---

**享受学习狼的世界！** 🐺🌍
